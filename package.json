{
  "name": "vpn-cx-proxy",
  "version": "1.0.0",
  "description": "Chrome & Firefox extension to toggle HTTP/SOCKS proxy",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "icons": "node scripts/generate-icons.js",
    "install-native-host": "node scripts/install-native-host.js"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "pngjs": "^7.0.0"
  }
}
